# Clean old builds (optional but good practice)
rm dist/
rm "vllama.egg-info"

pip uninstall vllama

# Rebuild the new version
python -m build


pip install dist/vllama-1.9.0-py3-none-any.whl

// Secondary Use commands

rm -rf dist/ 
rm -rf "vllama.egg-info"

pip3 uninstall vllama 

python3 -m build 

pip3 install dist/vllama-1.11.1-py3-none-any.whl

# Upload to PyPI
twine upload dist/*
